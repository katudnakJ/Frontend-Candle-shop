export interface Addresses {
  address_id: string;
  user_id: string;
  delivery_address: string;
  sub_district: string;
  district: string;
  province: string;
  postcode: string;
  address_label: string;
  is_default: boolean;
}